# Развёртывание Signaling Server на Render

## Шаг 1: Подготовка репозитория

Убедись, что у тебя есть:
- `signaling-server/server.js` - основной файл сервера
- `signaling-server/package.json` - зависимости
- `.gitignore` (не включай node_modules)

## Шаг 2: Создание аккаунта на Render

1. Перейди на [render.com](https://render.com)
2. Нажми "Get Started for Free"
3. Зарегистрируйся через GitHub (рекомендуется)

## Шаг 3: Создание Web Service

1. В Dashboard нажми **"New +"** → **"Web Service"**
2. Выбери свой GitHub репозиторий
3. Настройки:

```
Name: p2p-chat-signaling
Environment: Node
Region: Frankfurt (EU) или ближайший к тебе
Branch: main (или твоя ветка)
Build Command: cd signaling-server && npm install
Start Command: cd signaling-server && npm start
Plan: Free
```

## Шаг 4: Environment Variables

Добавь переменные окружения:

```
PORT=3000
NODE_ENV=production
```

## Шаг 5: Deploy

Нажми **"Create Web Service"**

Render автоматически:
- Соберёт проект
- Установит зависимости
- Запустит сервер
- Предоставит URL (например: `https://p2p-chat-signaling.onrender.com`)

## Шаг 6: Проверка

Открой в браузере:
```
https://p2p-chat-signaling.onrender.com/status
```

Должен вернуть:
```json
{
  "status": "online",
  "peers": 0,
  "rooms": 0,
  "uptime": 123.45
}
```

## Шаг 7: Обновление URL в приложении

В `lib/utils/constants.dart` замени:

```dart
static const String signalingServerUrl = 'https://p2p-chat-signaling.onrender.com';
```

## Важные моменты

### Бесплатный тариф (Free Tier):
- Сервер "засыпает" после 15 минут неактивности
- При первом запросе может быть задержка 30-60 секунд (cold start)
- 750 часов бесплатно в месяц (достаточно для тестирования)

### Чтобы сервер не "засыпал":
Можно настроить ping каждые 10 минут через cron-job.org или UptimeRobot

### Логи:
В Render Dashboard → Web Service → Logs можно видеть все запросы

### Перезапуск:
Если что-то пошло не так - нажми "Manual Deploy" → "Deploy latest commit"

## Альтернативы Render

Если Render не подходит:

1. **Railway** (railway.app) - тоже бесплатный tier
2. **Heroku** - бесплатный tier убрали, только платный
3. **Fly.io** - бесплатный tier с ограничениями
4. **Vercel** - только для serverless, не подходит для WebSocket
5. **Свой VPS** - DigitalOcean, Hetzner, etc.

## Готово! 🎉

После развёртывания твой сервер будет доступен 24/7 (с учётом ограничений free tier).

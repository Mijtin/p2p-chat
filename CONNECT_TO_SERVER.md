# Как подключиться к развёрнутому серверу

## Шаг 1: Получи URL сервера

После развёртывания на Render, у тебя будет URL типа:
```
https://p2p-chat-signaling.onrender.com
```

## Шаг 2: Обнови URL в приложении

Открой файл `lib/utils/constants.dart` и найди строку:

```dart
// ЗАМЕНИ на свой URL
static const String signalingServerUrl = 'https://p2p-chat-signaling.onrender.com';
```

**Важно:** Не забудь `/status` в конце для проверки!

## Шаг 3: Проверь статус сервера

Открой в браузере:
```
https://p2p-chat-signaling.onrender.com/status
```

Должен вернуть JSON:
```json
{
  "status": "online",
  "peers": 0,
  "rooms": 0,
  "uptime": 123.45
}
```

Если видишь это - сервер работает! 🎉

## Шаг 4: Пересобери приложение

### Windows:
```bash
flutter build windows --release
```

### Android:
```bash
flutter build apk --release
```

## Шаг 5: Запусти и тестируй

1. **Запусти приложение** на двух устройствах (или Windows + Android)
2. **На первом устройстве:**
   - Нажми "Создать комнату"
   - Запомни 6-значный код (например: `123456`)
3. **На втором устройстве:**
   - Нажми "Присоединиться"
   - Введи код `123456`
   - Нажми "Подключиться"
4. **Проверь логи:**
   - В терминале Windows: `flutter run`
   - В Android Studio: вкладка "Run"

## Шаг 6: Проверь соединение

Если всё работает, увидишь в логах:
```
✅ WebRTC: Data channel opened!
✅ Connected to peer
```

## Частые проблемы

### Проблема: "Сервер не отвечает"
**Решение:** 
- Проверь URL в `constants.dart`
- Убедись что сервер "проснулся" (бесплатный tier засыпает)
- Подожди 30-60 секунд и попробуй снова

### Проблема: "Не могу подключиться к peer"
**Решение:**
- Проверь что оба устройства в одной сети (или оба имеют интернет)
- Проверь firewall (Windows Defender может блокировать)
- Убедись что код введён правильно

### Проблема: "Data channel is not open"
**Решение:**
- Перезапусти оба приложения
- Создай новую комнату (новый код)
- Проверь логи сервера на Render (Dashboard → Logs)

## Проверка через curl

Тест сервера из командной строки:

```bash
curl https://p2p-chat-signaling.onrender.com/status
```

## Готово! 🎉

Если всё настроено правильно:
1. ✅ Сервер на Render работает
2. ✅ Приложение использует правильный URL
3. ✅ Два устройства могут соединяться через 6-значный код
4. ✅ Сообщения и файлы передаются напрямую (P2P)

**Теперь можно тестировать отправку сообщений, файлов и голосовых сообщений!**
